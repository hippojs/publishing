{{yield
  (hash
    publishPost=(route-action 'publishPost')
    updatePost=(route-action 'updatePost')

    openPublishModal=(action 'openPublishModal')
    closePublishModal=(action 'closePublishModal')
  )

  (hash
    showPublishModal=showPublishModal
  )
}}